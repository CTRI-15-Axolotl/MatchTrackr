import React from 'react';
import Card from '../dashboard/PlayerCard';

const CardDisplay = (props) => {
    
    return (
        <div> 
            <Card />
        </div>
    );
};

export default CardDisplay;